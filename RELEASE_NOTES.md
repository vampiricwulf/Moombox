### Bug Fixes

- Fix stream end time being set to download completion time for VODs/premieres — only live streams get a fallback stream end time now; VODs correctly leave it empty since there was no live broadcast
