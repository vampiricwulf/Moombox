### Internal

- **Refresh Go module dependencies and migrate `glamour` to v2.** Routine dep sweep — bubbles, bubbletea, lipgloss, sqlite, libc, x/{crypto,net,text,sys,term}, chroma, regexp2, and others bumped to current. `github.com/charmbracelet/glamour v0.8.0` → `charm.land/glamour/v2 v2.0.0` is a one-line import path change; all four API call sites in the release-notes overlay are unchanged across the major version. Side effects: OSC 8 clickable links in the release notes overlay (modern terminals), better CJK/emoji wrapping, and 5 fewer transitive deps (glamour v2 folds in `lipgloss/v2` + `cellbuf` which we already use, dropping `go-osc52`, `lipgloss` v1, `termenv`, and `reflow`).
- **Add Dependabot configuration** (`.github/dependabot.yml`) for weekly Go module + npm sidecar update PRs and monthly GitHub Actions PRs. Charm, `golang.org/x`, and `modernc.org` families are grouped so cascade bumps land as a single PR each instead of separate per-package PRs.
