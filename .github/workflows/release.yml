name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

# Three jobs, all on ubuntu-latest:
#
#   release  builds the embed blobs (sidecar + 3 Node binaries) once,
#            cached across runs by version.txt + sidecar manifest +
#            fetch-node source. Uploads them as the embed-blobs
#            artifact and creates the empty GitHub release.
#
#   windows  cross-compiles Moombox.exe on Linux via go-winres
#            (which works on any host OS), signs it, uploads to the
#            release.
#
#   linux    builds linux-amd64 + linux-arm64 in parallel within a
#            single shell, signs both, uploads to the release.
#
# windows + linux run in parallel after release. No more windows-latest
# runner — go-winres + go cross-compile produce identical Windows
# binaries from a Linux host (.syso files use filename build
# constraints, so go build picks them up under GOOS=windows).

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Restore embed blob cache
        # Cache key is the hash of: version.txt (encodes pinned Node
        # SHAs), the sidecar package-lock.json + build.mjs (encode
        # sidecar deps + build process), and tools/fetch-node/main.go
        # (encodes the fetch logic). When all four are unchanged
        # across runs the cached blobs are reused, skipping ~50s of
        # nodejs.org downloads + ~5s of npm ci. Cache evicts after
        # 7 days of disuse but releases run more frequently than that.
        id: cache-embed
        uses: actions/cache@v5
        with:
          path: |
            internal/bgutils/embed/sidecar.tar.gz
            internal/bgutils/embed/node-windows-amd64.gz
            internal/bgutils/embed/node-linux-amd64.gz
            internal/bgutils/embed/node-linux-arm64.gz
          key: embed-${{ hashFiles('internal/bgutils/embed/version.txt', 'bgutil-sidecar/package-lock.json', 'bgutil-sidecar/build.mjs', 'tools/fetch-node/main.go') }}

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - uses: actions/setup-node@v6
        if: steps.cache-embed.outputs.cache-hit != 'true'
        with:
          node-version: '22'

      - name: Build BotGuard sidecar payload
        # --ignore-scripts: a compromised dep's postinstall could
        # exfiltrate GITHUB_TOKEN; jsdom + bgutils-js have no install
        # scripts we depend on, so ignoring is safe.
        if: steps.cache-embed.outputs.cache-hit != 'true'
        run: |
          cd bgutil-sidecar
          npm ci --omit=dev --no-audit --no-fund --ignore-scripts
          node build.mjs
          cd ..

      - name: Fetch embedded Node binaries (all platforms)
        # Downloads pinned Node v22 LTS binaries for Windows x64, Linux x64,
        # and Linux arm64; verifies SHA-256; gzips to per-platform embed
        # files. Idempotent: skips re-download if version.txt matches.
        if: steps.cache-embed.outputs.cache-hit != 'true'
        run: go run ./tools/fetch-node

      - name: Upload embed blobs as artifact
        uses: actions/upload-artifact@v7
        with:
          name: embed-blobs
          path: |
            internal/bgutils/embed/sidecar.tar.gz
            internal/bgutils/embed/node-windows-amd64.gz
            internal/bgutils/embed/node-linux-amd64.gz
            internal/bgutils/embed/node-linux-arm64.gz
          retention-days: 1
          if-no-files-found: error

      - name: Build release body
        shell: bash
        env:
          TAG_NAME: ${{ github.ref_name }}
          REPO: ${{ github.repository }}
        run: |
          WIN_LINK="[**\`Download Moombox.exe for Windows (x64)\`**](https://github.com/${REPO}/releases/download/${TAG_NAME}/Moombox.exe)"
          LIN_AMD_LINK="[**\`Download moombox-linux-amd64 for Linux (x64)\`**](https://github.com/${REPO}/releases/download/${TAG_NAME}/moombox-linux-amd64)"
          LIN_ARM_LINK="[**\`Download moombox-linux-arm64 for Linux (arm64)\`**](https://github.com/${REPO}/releases/download/${TAG_NAME}/moombox-linux-arm64)"

          if [ -f RELEASE_NOTES.md ] && [ -s RELEASE_NOTES.md ]; then
            {
              echo "$WIN_LINK"
              echo "$LIN_AMD_LINK"
              echo "$LIN_ARM_LINK"
              echo ""
              echo "---"
              echo ""
              cat RELEASE_NOTES.md
            } > release_body.md
            echo "use_github_notes=false" >> "$GITHUB_ENV"
          else
            {
              echo "$WIN_LINK"
              echo "$LIN_AMD_LINK"
              echo "$LIN_ARM_LINK"
            } > release_body.md
            echo "use_github_notes=true" >> "$GITHUB_ENV"
          fi

      - name: Create GitHub Release (assets uploaded by parallel jobs)
        uses: softprops/action-gh-release@v3
        with:
          body_path: release_body.md
          generate_release_notes: ${{ env.use_github_notes == 'true' }}
          # Tags containing '-' (e.g. v2.6.0-test.1, v2.6.0-beta.2, v2.6.0-rc.1)
          # are treated as pre-releases: GitHub won't mark them "Latest" and the
          # /releases/latest endpoint continues to point at the last stable tag.
          prerelease: ${{ contains(github.ref_name, '-') }}
          make_latest: ${{ contains(github.ref_name, '-') && 'false' || 'true' }}
          # No files yet — the windows and linux jobs upload to this release
          # via tag-match in subsequent action-gh-release calls.

  windows:
    runs-on: ubuntu-latest
    needs: release
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Download embed blobs from release job
        uses: actions/download-artifact@v7
        with:
          name: embed-blobs
          path: internal/bgutils/embed/

      - name: Generate Windows resources
        # go-winres works on any host OS — it produces a Windows .syso
        # blob from declarative JSON. The .syso file uses filename build
        # constraints (rsrc_windows_amd64.syso) so go build picks it up
        # under GOOS=windows but ignores it on other platforms.
        #
        # winres.json is patched in-place with the version metadata via
        # jq (always available on ubuntu runners). Pre-release suffixes
        # like "-test.1" are stripped from the numeric version because
        # Windows resource metadata only accepts major.minor.patch.build.
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          COMMIT=$(git rev-parse --short HEAD)
          VERSION_STRING="${VERSION} (${COMMIT})"
          NUMERIC_BASE="${VERSION%%-*}"
          NUMERIC_VERSION="${NUMERIC_BASE}.0"

          go install github.com/tc-hib/go-winres@latest

          jq --arg ver "$NUMERIC_VERSION" --arg vs "$VERSION_STRING" '
            .RT_MANIFEST."#1"."0409".identity.version = $ver
            | .RT_VERSION."#1"."0000".fixed.file_version = $ver
            | .RT_VERSION."#1"."0000".fixed.product_version = $ver
            | .RT_VERSION."#1"."0000".info."0409".FileVersion = $vs
            | .RT_VERSION."#1"."0000".info."0409".ProductVersion = $vs
          ' cmd/moombox/winres/winres.json > cmd/moombox/winres/winres.json.tmp
          mv cmd/moombox/winres/winres.json.tmp cmd/moombox/winres/winres.json

          cd cmd/moombox
          go-winres make --arch amd64
          cd ../..

      - name: Build Windows executable (cross-compile from Linux)
        env:
          CGO_ENABLED: 0
          GOOS: windows
          GOARCH: amd64
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          COMMIT=$(git rev-parse --short HEAD)
          go build -ldflags "-s -w -X main.version=$VERSION -X main.commit=$COMMIT" -o Moombox.exe ./cmd/moombox

      - name: Sign binary
        env:
          SIGNING_KEY: ${{ secrets.SIGNING_KEY }}
        run: go run ./cmd/sign Moombox.exe

      - name: Upload Windows assets to release
        uses: softprops/action-gh-release@v3
        with:
          # Append to the release the `release` job created. Don't pass
          # body_path/prerelease/make_latest here — they would overwrite the
          # values set by the release job.
          files: |
            Moombox.exe
            Moombox.exe.sig

  linux:
    runs-on: ubuntu-latest
    needs: release
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Download embed blobs from release job
        uses: actions/download-artifact@v7
        with:
          name: embed-blobs
          path: internal/bgutils/embed/

      - name: Build Linux executables in parallel
        # Both arches build concurrently via shell job control. Go's
        # build cache handles concurrent invocations correctly and the
        # cross-compile is CPU-bound enough to benefit from parallelism
        # on the runner's 4 cores.
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          COMMIT=$(git rev-parse --short HEAD)
          LDFLAGS="-s -w -X main.version=$VERSION -X main.commit=$COMMIT"

          (CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -ldflags "$LDFLAGS" -o moombox-linux-amd64 ./cmd/moombox) &
          PID_AMD64=$!
          (CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -ldflags "$LDFLAGS" -o moombox-linux-arm64 ./cmd/moombox) &
          PID_ARM64=$!
          wait $PID_AMD64
          wait $PID_ARM64

      - name: Sign Linux binaries
        env:
          SIGNING_KEY: ${{ secrets.SIGNING_KEY }}
        run: |
          go run ./cmd/sign moombox-linux-amd64 &
          go run ./cmd/sign moombox-linux-arm64 &
          wait

      - name: Upload Linux assets to release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            moombox-linux-amd64
            moombox-linux-amd64.sig
            moombox-linux-arm64
            moombox-linux-arm64.sig
